/** Host loader entry for the marketplace browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the plugin marketplace UI. */
export function apply(): void {}
